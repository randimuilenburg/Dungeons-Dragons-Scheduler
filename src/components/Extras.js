// This componenent is only commented out code that I feel I might want but am too scared to just
// delete because I am a Junior. I will grow out of it eventually. But I want to clean up the code
// I am actually using. When I am feeling brave, I'll delete this whole thing.

// Link for registering, but as a button:
// const RegisterHereLink = () => {
//   return (
//     <Link to="/Login">
//       <Button variant="danger" size="xl">
//         Let's go.
//       </Button>
//     </Link>
//   );
// };

// API TEST CODE */
// /<div>
//   <p>`Testing something! Here is: ${testCode}.`</p>
// </div>

// API TEST CODE
// const playerName =

// const testCode = () => {
//   return `Here is ${apiData.players[0].character}!`;
// };

// UseEffect
// (onSubmit) = useEffect in Submit button
// use it to look up email, match email to user, console.log user to see if it works?

// Adding TEXT ON IMAGES hover:
// const ImageWithHoverText = ({ imagePath, hoverText }) => {
//   const [isHovered, setIsHovered] = useState(false);

//   const handleMouseOver = () => {
//     setIsHovered(true);
//   };

//   const handleMouseOut = () => {
//     setIsHovered(false);
//   };

//   return (
//     <div
//       onMouseOver={handleMouseOver}
//       onMouseOut={handleMouseOut}
//       style={{ display: 'inline-block' }}
//     >
//       <img src={imagePath} alt="Image" />
//       {isHovered && <div>{hoverText}</div>}
//     </div>
//   );
// };

// export default ImageWithHoverText;

// function App() {
//   const [isShown, setIsShown] = useState(false);
// }

// return (
//   <div className="App">
//     <button
//       onMouseEnter={() => setIsShown(true)}
//       onMouseLeave={() => setIsShown(false)}
//     >
//       Hover over me!
//     </button>
//     {isShown && <div>Hovering text!</div>}
//   </div>
// );
